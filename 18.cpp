#include <iostream>
#include"linklist.h"
using namespace std;

ListNode *swapPairs(ListNode *head)
{
    ListNode* temp = head;
    while(head->next == nullptr && head->next->next == nullptr)
    {
        int temp1 = temp->data;
    }
}

int main()
{

}